import { Typography } from "@mui/material";

export default function MentorDashboard() {
    return (
        <Typography variant="h2" fontWeight="bolder" textAlign="center" p="10rem">Thank you for signing up! Students will now be able to contact you when needed.</Typography>
    )
}